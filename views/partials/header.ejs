<header class="h-16 w-full bg-[#55142A] fixed top-0 left-0 flex items-center justify-between px-4 z-50">

  <!-- Logo -->
  <div class="text-white text-lg font-bold">Accomod</div>

  <!-- Desktop Search -->
  <div class="hidden md:flex">
    <form action="/search" method="post">
      <input
        type="search"
        name="query"
        placeholder="Search..."
        class="px-2.5 py-2 border text-white border-gray-300 rounded-md focus:outline-none text-sm"
      >
    </form>
  </div>

  <!-- Nav -->
<%- include('navbar.ejs') %>